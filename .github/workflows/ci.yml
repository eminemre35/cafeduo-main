name: CI/CD Pipeline

on:
  push:
    branches: [main, develop, 'feat/**', 'fix/**']
  pull_request:
    branches: [main, develop]

env:
  NODE_VERSION: '22.x'

jobs:
  # ==========================================
  # Job 1: Build & Unit Tests
  # ==========================================
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0  # Badge güncelleme için tam history gerekli

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'

      - name: Cache node_modules
        id: cache-node-modules
        uses: actions/cache@v6
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: npm ci

      - name: Run security audit (allowlist gate)
        run: node scripts/automation/audit-gate.mjs

      - name: Lint & Typecheck
        run: npm run quality

      - name: Backend syntax check
        run: |
          echo "Checking backend JavaScript syntax..."
          find backend -name "*.js" -exec node -c {} \; && echo "✅ Backend syntax OK"

      - name: Validate DB migrations
        run: |
          echo "Validating migration files..."
          if [ -d "migrations" ]; then
            for file in migrations/*.js; do
              if [ -f "$file" ]; then
                echo "Checking: $file"
                node -c "$file"
              fi
            done
            echo "✅ Migration files syntax OK"
          fi

      - name: Run Unit Tests with Coverage
        id: test
        run: npm run test:coverage -- --json --outputFile=coverage/test-results.json

      - name: Upload Coverage Report
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: coverage/
          retention-days: 30

      - name: Build
        run: npm run build

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: build-files
          path: dist/
          retention-days: 7

      # Coverage değerini environment variable olarak kaydet
      - name: Extract Coverage Stats
        id: coverage
        run: |
          if [ -f coverage/coverage-summary.json ]; then
            LINES_PCT=$(cat coverage/coverage-summary.json | jq -r '.total.lines.pct // 0')
            STATEMENTS_PCT=$(cat coverage/coverage-summary.json | jq -r '.total.statements.pct // 0')
            FUNCTIONS_PCT=$(cat coverage/coverage-summary.json | jq -r '.total.functions.pct // 0')
            BRANCHES_PCT=$(cat coverage/coverage-summary.json | jq -r '.total.branches.pct // 0')
          else
            LINES_PCT=0
            STATEMENTS_PCT=0
            FUNCTIONS_PCT=0
            BRANCHES_PCT=0
          fi
          
          echo "LINES_PCT=$LINES_PCT" >> $GITHUB_OUTPUT
          echo "STATEMENTS_PCT=$STATEMENTS_PCT" >> $GITHUB_OUTPUT
          echo "FUNCTIONS_PCT=$FUNCTIONS_PCT" >> $GITHUB_OUTPUT
          echo "BRANCHES_PCT=$BRANCHES_PCT" >> $GITHUB_OUTPUT
          
          # Toplam test sayısını çıkar (jest --json çıktısından)
          if [ -f coverage/test-results.json ]; then
            TESTS=$(jq -r '.numTotalTests // 0' coverage/test-results.json)
          else
            TESTS=0
          fi
          echo "TEST_COUNT=$TESTS" >> $GITHUB_OUTPUT
          
          # Badge rengini belirle
          if (( $(echo "$LINES_PCT >= 80" | bc -l) )); then
            COLOR="brightgreen"
          elif (( $(echo "$LINES_PCT >= 60" | bc -l) )); then
            COLOR="green"
          elif (( $(echo "$LINES_PCT >= 40" | bc -l) )); then
            COLOR="yellow"
          else
            COLOR="red"
          fi
          echo "BADGE_COLOR=$COLOR" >> $GITHUB_OUTPUT
          
          echo "📊 Coverage: Lines ${LINES_PCT}% | Statements ${STATEMENTS_PCT}% | Functions ${FUNCTIONS_PCT}% | Branches ${BRANCHES_PCT}%"

    outputs:
      lines_pct: ${{ steps.coverage.outputs.LINES_PCT }}
      statements_pct: ${{ steps.coverage.outputs.STATEMENTS_PCT }}
      functions_pct: ${{ steps.coverage.outputs.FUNCTIONS_PCT }}
      branches_pct: ${{ steps.coverage.outputs.BRANCHES_PCT }}
      badge_color: ${{ steps.coverage.outputs.BADGE_COLOR }}
      test_count: ${{ steps.coverage.outputs.TEST_COUNT }}

  # ==========================================
  # Job 2: E2E Tests (Playwright)
  # ==========================================
  e2e-tests:
    runs-on: ubuntu-latest
    needs: build-and-test
    timeout-minutes: 30
    # Resmi Playwright imajı: tarayıcılar + OS bağımlılıkları hazır gelir, böylece
    # `playwright install --with-deps` apt adımında 30 dk takılmaz (eski hata).
    container:
      image: mcr.microsoft.com/playwright:v1.58.1-jammy

    # E2E testler artık zorunlu (continue-on-error kaldırıldı)
    # Test stabilitesi için retry mekanizması kullanılıyor

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'

      - name: Restore node_modules cache
        id: cache-node-modules
        uses: actions/cache@v6
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}

      - name: Install dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: npm ci

      # Tarayıcılar imajda hazır; --with-deps gerekmez (apt takılmasını önler).
      - name: Install Playwright Browsers
        run: npx playwright install chromium

      - name: Run Playwright smoke E2E tests
        id: playwright
        run: npx playwright test --project=chromium --grep @smoke --reporter=list,html,junit
        env:
          PLAYWRIGHT_BASE_URL: http://127.0.0.1:3000
          E2E_API_BASE_URL: http://127.0.0.1:3001
          TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL || 'test@example.com' }}
          TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD || 'testpassword123' }}

      - name: Upload Playwright Report
        uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Upload Playwright Screenshots
        uses: actions/upload-artifact@v7
        if: ${{ failure() }}
        with:
          name: playwright-screenshots
          path: test-results/
          retention-days: 7

      - name: Parse Test Results
        if: always()
        id: e2e-results
        run: |
          if [ -f playwright-report/results.xml ]; then
            PASSED=$(grep -o 'testsuite.*tests="[0-9]*"' playwright-report/results.xml | grep -o 'tests="[0-9]*"' | grep -o '[0-9]*' | awk '{s+=$1} END {print s}')
            FAILURES=$(grep -o 'testsuite.*failures="[0-9]*"' playwright-report/results.xml | grep -o 'failures="[0-9]*"' | grep -o '[0-9]*' | awk '{s+=$1} END {print s}')
            SKIPPED=$(grep -o 'testsuite.*skipped="[0-9]*"' playwright-report/results.xml | grep -o 'skipped="[0-9]*"' | grep -o '[0-9]*' | awk '{s+=$1} END {print s}')
            echo "PASSED=${PASSED:-0}" >> $GITHUB_OUTPUT
            echo "FAILED=${FAILURES:-0}" >> $GITHUB_OUTPUT
            echo "SKIPPED=${SKIPPED:-0}" >> $GITHUB_OUTPUT
          else
            echo "PASSED=0" >> $GITHUB_OUTPUT
            echo "FAILED=0" >> $GITHUB_OUTPUT
            echo "SKIPPED=0" >> $GITHUB_OUTPUT
          fi

    outputs:
      passed: ${{ steps.e2e-results.outputs.PASSED }}
      failed: ${{ steps.e2e-results.outputs.FAILED }}
      skipped: ${{ steps.e2e-results.outputs.SKIPPED }}

  # ==========================================
  # Job 3: Test Summary PR Comment
  # ==========================================
  pr-comment:
    runs-on: ubuntu-latest
    needs: [build-and-test, e2e-tests]
    if: github.event_name == 'pull_request'
    permissions:
      pull-requests: write
    
    steps:
      - name: Comment PR with Test Summary
        uses: actions/github-script@v9
        with:
          script: |
            const lines = '${{ needs.build-and-test.outputs.lines_pct }}';
            const statements = '${{ needs.build-and-test.outputs.statements_pct }}';
            const functions = '${{ needs.build-and-test.outputs.functions_pct }}';
            const branches = '${{ needs.build-and-test.outputs.branches_pct }}';
            const badgeColor = '${{ needs.build-and-test.outputs.badge_color }}';
            
            const e2ePassed = '${{ needs.e2e-tests.outputs.passed }}';
            const e2eFailed = '${{ needs.e2e-tests.outputs.failed }}';
            const e2eSkipped = '${{ needs.e2e-tests.outputs.skipped }}';
            
            const statusIcon = (e2eFailed === '0') ? '✅' : '❌';
            const e2eStatus = (e2eFailed === '0') ? 'Passed' : 'Failed';
            
            const body = `## 🧪 CafeDuo Test Report
            
            ### 📊 Coverage Report
            | Metric | Value |
            |--------|-------|
            | **Lines** | ${lines}% |
            | **Statements** | ${statements}% |
            | **Functions** | ${functions}% |
            | **Branches** | ${branches}% |
            
            ![Coverage](https://img.shields.io/badge/coverage-${lines}%25-${badgeColor})
            
            ### 🎭 E2E Tests (Playwright Smoke)
            ${statusIcon} **${e2eStatus}**
            - ✅ Passed: ${e2ePassed}
            - ❌ Failed: ${e2eFailed}
            - ⏭️ Skipped: ${e2eSkipped}
            
            ### 📦 Artifacts
            - [View Coverage Report](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            - [View E2E Report](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            
            ---
            *Generated by GitHub Actions*`;
            
            // Önceki yorumları bul ve güncelle
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number
            });
            
            const botComment = comments.find(comment => 
              comment.user.type === 'Bot' && 
              comment.body.includes('🧪 CafeDuo Test Report')
            );
            
            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }

  # ==========================================
  # Job 4: Update Coverage Badge (Main Branch)
  # ==========================================
  update-badge:
    runs-on: ubuntu-latest
    needs: [build-and-test, e2e-tests]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update README Badge
        run: |
          COVERAGE="${{ needs.build-and-test.outputs.lines_pct }}"
          COLOR="${{ needs.build-and-test.outputs.badge_color }}"
          TESTS="${{ needs.build-and-test.outputs.test_count }}"
          
          echo "Updating coverage badge to: ${COVERAGE}% (${COLOR}), tests: ${TESTS}"
          
          # README.md'deki coverage badge'ini güncelle
          # Format: ![Coverage](https://img.shields.io/badge/coverage-XX%25-color)
          sed -i "s|https://img.shields.io/badge/coverage-[0-9.]*%25-[a-z]*|https://img.shields.io/badge/coverage-${COVERAGE}%25-${COLOR}|g" README.md
          
          # Test sayısı badge'ini güncelle (hardcoded kalmıştı)
          # Format: ![Tests](https://img.shields.io/badge/tests-N%20passing-...)
          sed -i "s|https://img.shields.io/badge/tests-[0-9]*%20passing|https://img.shields.io/badge/tests-${TESTS}%20passing|g" README.md

      - name: Commit Badge Update
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          if git diff --quiet README.md; then
            echo "No changes to README.md"
          else
            git add README.md
            git commit -m "📊 Update badges: coverage ${{ needs.build-and-test.outputs.lines_pct }}%, tests ${{ needs.build-and-test.outputs.test_count }}"
            git push
          fi
